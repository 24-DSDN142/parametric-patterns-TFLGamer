You will need to fill out this file for hand in. 

The bellow is just an example, feel free to disregard this documents contents.


Your final settings.txt will look something like this when you are finished:

/////////////////////////////////

Wallpaper 1:

//your parameter variables go here!
let wallpaper = 0; // colour palette of wallpaper
let catScale = 0.6; // scale cat glyph
let fruitScale = 1; // scale fruit glyph
let pattern = 2; // switch between cat and fruit patterns
let catFaceHeight = 0; // height of cat faces


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 2:

//your parameter variables go here!
let wallpaper = 1; // colour palette of wallpaper
let catScale = 0.6; // scale cat glyph
let fruitScale = 1; // scale fruit glyph
let pattern = 2; // switch between cat and fruit patterns
let catFaceHeight = 0; // height of cat faces


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 3:

//your parameter variables go here!
let wallpaper = 2; // colour palette of wallpaper
let catScale = 0.6; // scale cat glyph
let fruitScale = 1; // scale fruit glyph
let pattern = 2; // switch between cat and fruit patterns
let catFaceHeight = 0; // height of cat faces


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 4:

//your parameter variables go here!
let wallpaper = 2; // colour palette of wallpaper
let catScale = 0.6; // scale cat glyph
let fruitScale = 1.86; // scale fruit glyph
let pattern = 2; // switch between cat and fruit patterns
let catFaceHeight = 0; // height of cat faces


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 5:

//your parameter variables go here!
let wallpaper = 0; // colour palette of wallpaper
let catScale = 0.6; // scale cat glyph
let fruitScale = 1; // scale fruit glyph
let pattern = 0; // switch between cat and fruit patterns
let catFaceHeight = 18; // height of cat faces


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 6:

//your parameter variables go here!
let wallpaper = 1; // colour palette of wallpaper
let catScale = 1; // scale cat glyph
let fruitScale = 0.5; // scale fruit glyph
let pattern = 1; // switch between cat and fruit patterns
let catFaceHeight = 0; // height of cat faces


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 7:

//your parameter variables go here!
let wallpaper = 1; // colour palette of wallpaper
let catScale = 0.5; // scale cat glyph
let fruitScale = 1.5; // scale fruit glyph
let pattern = 2; // switch between cat and fruit patterns
let catFaceHeight = -5; // height of cat faces


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 8:

//your parameter variables go here!
let wallpaper = 2; // colour palette of wallpaper
let catScale = 2; // scale cat glyph
let fruitScale = 1.5; // scale fruit glyph
let pattern = 2; // switch between cat and fruit patterns
let catFaceHeight = -5; // height of cat faces


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 9:

//your parameter variables go here!
let wallpaper = 0; // colour palette of wallpaper
let catScale = 2; // scale cat glyph
let fruitScale = 1.2; // scale fruit glyph
let pattern = 2; // switch between cat and fruit patterns
let catFaceHeight = 20; // height of cat faces


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}